/** @type {import('next').NextConfig} */
module.exports = {
    basePath: '/portfolio',
    reactStrictMode: true,
    output: 'export',
    images: {
      unoptimized: true,  // Disable image optimization
    },
  }
  
